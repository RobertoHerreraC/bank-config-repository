server:
  port: 8085

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/movementdb

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

logging:
  level:
    root: INFO
    com.bank.movement: DEBUG